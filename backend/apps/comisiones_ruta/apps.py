from django.apps import AppConfig


class ComisionesRutaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.comisiones_ruta'
    verbose_name = 'Comisiones por colaborador'
